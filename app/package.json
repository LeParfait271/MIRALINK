{
  "name": "miralink-app",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/protocol.js && node --check src/storage.js && node --check src/i18n.js",
    "test": "node --test tests/*.test.mjs"
  }
}
